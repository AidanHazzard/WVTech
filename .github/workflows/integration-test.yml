name: Integration Test

on: 
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src/MealPlanner
    steps:
      - uses: actions/checkout@v5
      - name: Setup dotnet
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.x'
      - name: Install dependencies
        run: dotnet restore ./MealPlanner.Tests
      - name: Build test project
        run: dotnet build ./MealPlanner.Tests --no-restore
      - name: Test
        run: dotnet test ./MealPlanner.Tests --no-build
  
